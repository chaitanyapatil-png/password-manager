[--Run this all commands in command prompt in same folder--]

# 1. Clone the repository (or download ZIP)
git clone https://github.com/your-username/password-manager.git
cd password-manager

# 2. Create and activate virtual environment (optional but recommended)
python -m venv venv
venv\Scripts\activate       # On Windows
# source venv/bin/activate  # On Linux/macOS

# 3. Install dependencies
pip install -r requirements.txt

# 4. Run the app (in development)
python main.py

# 5. Create .exe with PyInstaller (with app icon)
pyinstaller --onefile --windowed --icon=icon.ico main.py

# 6. Navigate to the EXE output folder
cd dist

# 7. Optional: Compress the .exe using UPX (after downloading UPX)
C:\path\to\upx.exe main.exe

# 8. Optional: Create a .zip for GitHub upload
# Right-click > Send to > Compressed (zipped) folder


Python must be installed (if running the .py version).

You must include icon.ico in the same directory or update the path in your PyInstaller command.
